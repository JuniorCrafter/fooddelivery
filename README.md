🍕 Food Delivery Microservices

Профессиональная микросервисная платформа для заказа и доставки еды, построенная на языке Go. Проект демонстрирует современный подход к бэкенд-разработке: разделение ответственности, работу с транзакционными и in-memory базами данных, а также событийную архитектуру.
🏗 Архитектура системы

Система состоит из 7 независимых компонентов, объединенных через API Gateway:

    Auth Service (:8081): Регистрация и аутентификация пользователей с использованием JWT и ролевой модели (RBAC: клиент, курьер, админ).

    Catalog Service (:8080): Управление меню товаров, описаниями и ценами.

    Order Service (:8082): Оформление заказов и управление транзакциями в БД.

    Courier Service (:8083): Личный кабинет курьера, история доставок и расчет заработка.

    Geo Service (:8084): Отслеживание координат курьеров в реальном времени и расчет дистанций.

    Notification Service: Асинхронная обработка уведомлений через брокер сообщений.

    API Gateway (:8000): Единая точка входа для фронтенда, обеспечивающая маршрутизацию запросов. 

🛠 Технологический стек

    Язык: Go (Golang) — высокая производительность и конкурентность.

    База данных: PostgreSQL — надежное хранение заказов и пользователей.

    Кэширование/Гео: Redis — сверхбыстрая работа с координатами.

    Брокер сообщений: RabbitMQ — асинхронная связь между сервисами.

    Инфраструктура: Docker & Docker Compose — полная контейнеризация.

    Документация: Swagger (OpenAPI) — интерактивная карта API. 

🚀 Быстрый запуск
Предварительные требования

    Установленный Docker и Docker Compose.

    Файл .env в корне проекта (см. .env.example).

Команда запуска

Развертывание всей инфраструктуры и 7 микросервисов одной командой:
Bash

docker compose up -d --build

После запуска:

    API Gateway: http://localhost:8000

    Swagger UI: http://localhost:8000/swagger/index.html

    RabbitMQ Admin: http://localhost:15672 (guest/guest)

    PostgreSQL: localhost:5432

📡 Основные эндпоинты (через Gateway)
Метод	Путь	Описание
POST	/auth/register	Регистрация (client/courier/admin)
POST	/auth/login	Вход и получение JWT-токена
POST	/catalog/products	Добавление товара (требует Auth)
POST	/orders/orders	Создание заказа
POST	/courier/accept	Принятие заказа курьером
GET	/courier/dashboard/{id}	Статистика и заработок курьера
POST	/geo/update	Отправка GPS-координат курьера
🛠 Решение типичных проблем (из опыта разработки)

В ходе работы над проектом были решены ключевые инженерные вызовы:

    Race Condition (Ошибки подключения): Внедрены healthchecks в Docker Compose. Сервисы ждут статуса (healthy) от PostgreSQL и Redis, прежде чем начать работу. Это устранило ошибки "Connection refused".

    Сетевая изоляция Docker: Все запросы внутри Gateway переведены с localhost на внутренние имена сервисов (auth-service, order-service и т.д.), что решило проблему 502 Bad Gateway.

    Контрактное программирование: Строгое соблюдение интерфейсов Go гарантирует, что методы (например, GetDashboard) всегда соответствуют своим сигнатурам.

    Целостность данных: Использование docker compose down -v и файлов инициализации (db/init/001_init.sql) обеспечивает чистый запуск базы со всеми необходимыми связями.

Разработчик: JuniorCrafter Лицензия: MIT
